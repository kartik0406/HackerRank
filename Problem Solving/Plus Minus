void plusMinus(vector<int> arr) {
    int n=arr.size();
int pos=0,neg=0;
for(int i=0;i<n;i++){
    if(arr[i]>0){
    pos++;
    }
    else if(arr[i]<0)
    neg++;
}
double pratio=(double)pos/(double)n;
