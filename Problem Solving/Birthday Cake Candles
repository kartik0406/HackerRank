int birthdayCakeCandles(vector<int> candles){
int n=candles.size();
int count=0;
int max=*max_element(candles.begin(), candles.end());
for(int i=0;i<n;i++){
    if(candles[i]==max) {
        count++;
      
    }
}
return count;
}
